import React from 'react';
import classes from "./Toolbar.css";
const toolbar =(props)=>(
    <header className={classes.Toolbar}>
        menu logo nav(contians navigation links)
    </header>
)

export default toolbar;